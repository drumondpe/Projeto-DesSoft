import pygame
import random

pygame.init()

pygame.init()
posicao = pygame.math.Vector2
LARGURA = 

class Jogo:
    def __init__(self):
        self.screen = pygame.display.set_mode((LARGURA, ALTURA))
